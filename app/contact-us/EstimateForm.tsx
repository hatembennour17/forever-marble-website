"use client";
import { FormEvent } from "react";

export default function EstimateForm(){
  function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const body=[`Name: ${form.get("name")}`,`Phone: ${form.get("phone")}`,`Email: ${form.get("email")}`,`Project type: ${form.get("project")}`,`Material: ${form.get("material")}`,`Message: ${form.get("message")}`].join("\n");window.location.href=`mailto:granite@forevermarble.com?subject=${encodeURIComponent("Countertop estimate request")}&body=${encodeURIComponent(body)}`;}
  return <form className="estimate-form" onSubmit={submit}><div className="form-row"><label>First and last name<input name="name" required autoComplete="name"/></label><label>Phone<input name="phone" required type="tel" autoComplete="tel"/></label></div><label>Email address<input name="email" required type="email" autoComplete="email"/></label><div className="form-row"><label>Project type<select name="project" defaultValue="Kitchen"><option>Kitchen</option><option>Bathroom</option><option>Fireplace</option><option>Bar or other</option></select></label><label>Material of interest<select name="material" defaultValue="Not sure yet"><option>Not sure yet</option><option>Granite</option><option>Marble</option><option>Quartz</option><option>Quartzite</option></select></label></div><label>Tell us about your project<textarea name="message" rows={5} placeholder="Approximate dimensions, timeline, colors you like…"/></label><button className="button gold" type="submit">Send estimate request</button><small>This opens your email app with your project details ready to send.</small></form>;
}
